use slex.*

class Node<T>:
    T data
    
    def Node(T a, T b):
        self.data = a + b

def main(): i32
    Node<i32>* node = new Node<i32>(1, 2)
    
    print(node.data)
    
    del node
    return 0
